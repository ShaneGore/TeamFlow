# Copy to .env for local development.
# Beginner-friendly default: local SQLite file, auto-created on startup.
# Replace with another SQLAlchemy URL later (e.g. postgresql+psycopg://...).
DATABASE_URL=sqlite:///./teamflow.db
FRONTEND_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
