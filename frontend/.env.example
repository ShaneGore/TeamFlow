# Copy to .env for local development.
# The frontend always talks to the FastAPI backend over HTTP.
# Use this exact backend origin unless you move the backend.
VITE_API_BASE_URL=http://localhost:8000
